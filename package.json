{
  "name": "alquran_id",
  "version": "1.0.0",
  "description": "A sample command-line application with an entrypoint in `bin/`, library code in `lib/`, and example unit test in `test/`.",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wisnu G. Saputra",
  "license": "ISC",
  "devDependencies": {
    "honkit": "^4.0.7"
  },
  "dependencies": {}
}
